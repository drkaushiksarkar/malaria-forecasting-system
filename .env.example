# ---- Core ----
APP_ENV=prod
APP_PORT=8080

# ---- Database ----
DATABASE_URL=postgresql+psycopg2://malaria_user:malaria_pass@localhost:5432/malaria

# ---- Model / Paths ----
MODEL_ROOT=/app/models
STREAM_DATA_PATH=/app/data/stream
VERIFICATION_PATH=/app/data/verification

# ---- Scheduler ----
CRON_EXPRESSION=*/30 * * * *   # every 30 minutes

# ---- Security (FastAPI docs auth is optional; leave blank if not used) ----
BASIC_AUTH_USER=
BASIC_AUTH_PASS=